"""Business logic layer (added by module developers).

Rule: business logic lives in services, never inside route handlers. Future
trust modules add e.g. sealing, evidence, validation, verdict, vpr services.
"""
