import Home from "./pages/Home";

/**
 * PRAMAAN — Prove Once. Reuse the Proof.
 *
 * Foundation phase: single landing page. Future module routes/shells are
 * added by the owning developers.
 */
export default function App() {
  return <Home />;
}
